{plugins, [pc]}.

{provider_hooks,
 [
  {pre,
   [
    {"(linux|darwin|solaris|freebsd|netbsd|openbsd)",
    {compile, {pc, compile}},
    {clean, {pc, clean}},
    {"win32"}
   ]
  }
 ]
}.

{port_specs, [{"priv/inotify", ["c_src/*.c"]}]}.

{cover_enabled, true}.
